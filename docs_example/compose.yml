services:
  mkdocs-serve:
    build:
      context: .
      dockerfile: Containerfile
    volumes:
      - ./:/docs
    ports:
      - 8000:8000

  mkdocs-build:
    build:
      context: .
      dockerfile: Containerfile
    volumes:
      - ./:/docs
    command: build
