services:
  mkdocs-serve:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/docs
    ports:
      - 8000:8000

  mkdocs-build:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/docs
    command: build
