{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mkdocs serve",
            "type": "shell",
            "command": "pip install .. && mkdocs serve --dev-addr=0.0.0.0:8000",
            "options": {
                "cwd": "${workspaceFolder}/docs_example/"
            },
        },
    ]
}
